name: libdivecomputer
description: "Wrapper for libdivecomputer into Flutter"
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.10.4

dependencies:
  btproto: {path: ../btproto}
  code_assets: ^1.0.0
  ffi: ^2.2.0
  glob: ^2.1.3
  hooks: ^1.0.0
  logging: ^1.3.0
  native_toolchain_c: ^0.17.1

dev_dependencies:
  build_runner: ^2.15.0
  ffigen: ^20.1.1
  test: ^1.31.1
